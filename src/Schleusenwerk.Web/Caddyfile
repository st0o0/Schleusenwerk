:3000 {
	handle /api/* {
		reverse_proxy proxy:5000
	}

	handle /hubs/* {
		reverse_proxy proxy:5000
	}

	handle /health {
		respond "ok" 200
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}

	encode gzip zstd
}
